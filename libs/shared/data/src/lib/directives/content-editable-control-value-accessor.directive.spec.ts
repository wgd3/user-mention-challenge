import { ContentEditableControlValueAccessorDirective } from './content-editable-control-value-accessor.directive';

describe('ContentEditableControlValueAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentEditableControlValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
