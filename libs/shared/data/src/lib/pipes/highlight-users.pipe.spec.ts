import { HighlightUsersPipe } from './highlight-users.pipe';

describe('HighlightUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
